import fs from 'fs'; import pkg from 'pg'; import 'dotenv/config'; const {Pool}=pkg; const pool=new Pool({host:process.env.PGHOST,port:process.env.PGPORT,database:process.env.PGDATABASE,user:process.env.PGUSER,password:process.env.PGPASSWORD}); const sql=fs.readFileSync(new URL('./../sql/schema.sql', import.meta.url),'utf-8'); await pool.query(sql); console.log('Migrated'); process.exit(0);